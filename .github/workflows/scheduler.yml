name: Daily Fact YouTube Shorts Creator

on:
  schedule:
    # 09:00 AM UTC - Morning Rush
    - cron: '0 9 * * *'
    # 01:00 PM UTC - Lunch Break
    - cron: '0 13 * * *'
    # 05:00 PM UTC - Evening Rush
    - cron: '0 17 * * *'
    # 09:00 PM UTC - Late Night Scroll
    - cron: '0 21 * * *'
  workflow_dispatch:

jobs:
  build-and-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg fonts-dejavu-core

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Shorts Creator
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TOKEN_JSON: ${{ secrets.TOKEN_JSON }}
        run: python main.py

      - name: Keep Alive
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          date > keep_alive.txt
          git add keep_alive.txt
          git commit -m "Keep alive: $(date)" || exit 0
          git push
