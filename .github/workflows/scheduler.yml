name: AI Shorts Creator

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6  * * *'

jobs:
  run-creator:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Fix Network & Install System Dependencies
        run: |
          # IPv6 takılmalarını engellemek için IPv4 zorunlu kılındı
          echo 'Acquire::ForceIPv4 "true";' | sudo tee /etc/apt/apt.conf.d/99force-ipv4
          echo 'Acquire::http::Timeout "10";' | sudo tee /etc/apt/apt.conf.d/99timeout
          sudo apt-get update -y -q
          sudo apt-get install -y --no-install-recommends ffmpeg imagemagick

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install "moviepy>=2.0.0" imageio-ffmpeg huggingface_hub google-genai google-api-python-client google-auth-oauthlib gradio_client

      - name: Run Main Script
        env:
          YT_API_KEY: ${{ secrets.YT_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          TOKEN_JSON: ${{ secrets.TOKEN_JSON }}
        run: python main.py
